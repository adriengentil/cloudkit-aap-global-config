apiVersion: batch/v1
kind: Job
metadata:
  name: cloudkit-aap-booststrap-job
spec:
  template:
    spec:
      containers:
      - name: boostrap
        image: ghcr.io/adriengentil/cloudkit-app-bootstrap:latest
      restartPolicy: Never
  backoffLimit: 4
